# Pour lancer l'API:

1. utiliser la command cd pour definiir api comme repertoire de travail
2. CMD: kubectl create -f fastapi-service.yml
3. CMD: kubectl create -f fastapi-deployment.yml
4. Attendre le demarrage de l'api peut prendre un certain temps (jusqu'a 5-10 min)